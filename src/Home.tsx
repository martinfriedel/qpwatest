import React from "react";

const Home: React.FC = () => (
  <>
  <h1>Qubidu PWA.</h1>
  <h2>Very boring so far.</h2>
  <p>There is a list which is stored in localStorage and you can add random strings to it.</p>
  <p>To do that go to items screen. </p>
  <p>You should get  a prompt to install this on your home screen, which you can do and it should work and retain its data even if you quit the app and are offline and restart again.</p>
  </>
);

export default Home;